name: Terraform Deploy (PR -> dev)

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'terraform/**'
      - 'download_and_upload/**'
      - 'convert_log_to_parquet/**'
      - 'layer/**'
      - 'build.sh'
      - 'docker/dbt/**'
      - 'dbt_profiles/**'
      - 'scripts/build_dbt_image.sh'

jobs:
  deploy-dev:
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:iam::143944071087:role/GitHubActionsTerraformDeployRole
          aws-region: ap-northeast-1

      - name: Build Lambda Deployment Packages
        run: bash build.sh dev

      - name: Build and Push dbt Image
        env:
          DBT_IMAGE_TAG: dev-${{ github.sha }}
        run: |
          SHORT_TAG="${DBT_IMAGE_TAG:0:12}"
          bash scripts/build_dbt_image.sh dev "${SHORT_TAG}"
          echo "DBT_IMAGE_TAG=${SHORT_TAG}" >> "$GITHUB_ENV"

      - name: Terraform Apply (IAM First)
        run: |
          cd terraform
          docker compose build
          docker compose run --rm \
            -e AWS_ACCESS_KEY_ID \
            -e AWS_SECRET_ACCESS_KEY \
            -e AWS_SESSION_TOKEN \
            -e AWS_DEFAULT_REGION \
            -e AWS_REGION \
            terraform sh -c "\
              terraform init -reconfigure && \
              (terraform workspace select dev || terraform workspace new dev) && \
              terraform apply -auto-approve -var-file=dev.tfvars \
                -target=aws_iam_role_policy.github_actions_terraform_deploy_policy \
                -target=aws_iam_role_policy.sfn_execution_policy"

      - name: Terraform Apply (All)
        run: |
          cd terraform
          docker compose run --rm \
            -e AWS_ACCESS_KEY_ID \
            -e AWS_SECRET_ACCESS_KEY \
            -e AWS_SESSION_TOKEN \
            -e AWS_DEFAULT_REGION \
            -e AWS_REGION \
            terraform sh -c "\
              terraform apply -auto-approve -var-file=dev.tfvars"
