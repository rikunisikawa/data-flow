==============================================================
Transform Blueprint — 変換設計書
==============================================================
依頼識別子: [SLUG]
作成日:     [DATE]
対象レイヤ: [raw → stage / stage → processed / TBD]

[注意: このドキュメントは設計のみ。変換の実行はL3承認後に人間が実施する]

--------------------------------------------------------------
1. 変換概要
--------------------------------------------------------------
変換の目的:  [例: ログファイルをParquetに変換し、分析可能な形式にする]
変換ツール:  [TBD — dbt / Glue / Spark / SQL / Python 等から選択。実装者が決定]
スケジュール: [TBD — 日次バッチ / イベント駆動 / 手動]

--------------------------------------------------------------
2. 変換ステップ
--------------------------------------------------------------
Step 1: 入力データの取得
  - ソース: [s3://bucket/raw/ または [source_table]]
  - 形式:   [log / CSV / JSON]
  - フィルタ条件: [例: 対象期間のファイルのみ / TBD]
  [実行禁止: Step 1はデータ読み込みのため、実行はL2承認後]

Step 2: データクレンジング
  - NULL処理:
    - [col_a]: NULL → [デフォルト値 / 除外 / フラグ立て]
    - [col_b]: NULL許容（後続集計時に除外）
  - 型変換:
    - [col_c]: 文字列 → 数値（変換失敗時: 除外 / NULL化）
  - 重複排除:
    - 方針: [例: timestamp降順でROW_NUMBER()=1を採用]

Step 3: 変換・加工
  - 集計: [例: 被験者×活動ラベルごとの平均センサ値を計算]
  - 結合: [source_a] JOIN [source_b] ON [join_key]
  - 派生カラム:
    - [new_col] = [計算式または説明]

Step 4: 出力先への書き込み
  - 出力先: [s3://bucket/stage/ または [target_table]]
  - 形式:   [Parquet]
  - パーティション: [例: year=/month=/day=]
  - 書き込みモード: [上書き / 追記]
  [実行禁止: Step 4は書き込み操作のため、実行はL3承認後]

--------------------------------------------------------------
3. 命名規約
--------------------------------------------------------------
テーブル名:   [snake_case。プレフィックス: stg_ / int_ / fct_ / dim_]
カラム名:     [snake_case。略語禁止（例: cnt → count）]
ファイル名:   [YYYYMMDD_[slug]_[layer].parquet]

--------------------------------------------------------------
4. エラー処理方針
--------------------------------------------------------------
変換エラー時:
  - ログに記録する（ログ先: [TBD]）
  - 許容エラー率: [< 1%]（超過でパイプライン停止）
  - 担当者に通知する（通知先: [TBD]）

--------------------------------------------------------------
5. 品質チェックポイント
--------------------------------------------------------------
Step 2完了後: 件数確認（ソースとのレコード数差分チェック）
Step 4完了後: 出力スキーマの契約確認（data_contract.yamlと照合）

[実行禁止: これらのチェックもL2承認後にエンジニアが実施する]

==============================================================
