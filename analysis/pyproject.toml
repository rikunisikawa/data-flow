[project]
name = "analysis-env"
version = "0.1.0"
description = "Data analysis workspace template"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
